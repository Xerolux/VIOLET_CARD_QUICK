# 🤝 Mitwirken bei der Violet Pool Card

Wir freuen uns über jeden Beitrag zur Violet Pool Card! Egal ob Bugfixes, neue Funktionen oder Verbesserungen der Dokumentation - jede Hilfe ist willkommen.

## 🛠️ Entwicklung

### Voraussetzungen

*   Node.js und npm installiert
*   Git installiert

### Setup

1.  **Repository klonen:**
    ```bash
    git clone https://github.com/Xerolux/violet-pool-card.git
    cd violet-pool-card
    ```

2.  **Abhängigkeiten installieren:**
    ```bash
    npm install
    ```

3.  **Build:**
    ```bash
    npm run build
    ```

4.  **Entwicklungsserver starten (mit Auto-Reload):**
    ```bash
    npm run watch
    ```

### Lokales Testen

Um die Karte während der Entwicklung zu testen, können Sie den Entwicklungsserver nutzen:

```bash
npm run serve
```

Die Karte ist dann unter `http://localhost:5000/violet-pool-card.js` verfügbar. Fügen Sie diese URL zu Ihren Home Assistant Ressourcen hinzu.

### Pull Requests

1.  Forken Sie das Repository.
2.  Erstellen Sie einen Feature-Branch (`git checkout -b feature/tolles-feature`).
3.  Committen Sie Ihre Änderungen (`git commit -m 'Add tolles feature'`).
4.  Pushen Sie zum Branch (`git push origin feature/tolles-feature`).
5.  Öffnen Sie einen Pull Request.

Bitte stellen Sie sicher, dass Ihr Code den bestehenden Stilrichtlinien folgt und keine Fehler im Build-Prozess verursacht.

---

# 🇬🇧 Contributing to Violet Pool Card

We welcome contributions to the Violet Pool Card! Whether it's bug fixes, new features, or documentation improvements - every bit of help is appreciated.

## 🛠️ Development

### Prerequisites

*   Node.js and npm installed
*   Git installed

### Setup

1.  **Clone the repository:**
    ```bash
    git clone https://github.com/Xerolux/violet-pool-card.git
    cd violet-pool-card
    ```

2.  **Install dependencies:**
    ```bash
    npm install
    ```

3.  **Build:**
    ```bash
    npm run build
    ```

4.  **Start development server (with auto-reload):**
    ```bash
    npm run watch
    ```

### Local Testing

To test the card during development, you can use the development server:

```bash
npm run serve
```

The card will be available at `http://localhost:5000/violet-pool-card.js`. Add this URL to your Home Assistant resources.

### Pull Requests

1.  Fork the repository.
2.  Create a feature branch (`git checkout -b feature/amazing-feature`).
3.  Commit your changes (`git commit -m 'Add amazing feature'`).
4.  Push to the branch (`git push origin feature/amazing-feature`).
5.  Open a Pull Request.

Please ensure your code follows existing style guidelines and does not cause errors in the build process.
